{% extends "layout.html.twig" %}

{% block content %}
    <table>
        <tr>
            <th>region</th>
        </tr>

        {% for region in regions %}
            <tr>
                <td><a href="{{ path('region_index', {id_region: region.id_region }) }}">{{ region.region_name }}</a>
                </td>
            </tr>
        {% endfor %}

    </table>
{% endblock %}
